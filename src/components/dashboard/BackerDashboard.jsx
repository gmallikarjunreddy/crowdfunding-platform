import React from 'react';

const BackerDashboard = () => {
    return (
        <div>
            <h2>Backer Dashboard</h2>
            {/* Add backed projects list, donation history, etc. */}
        </div>
    );
};

export default BackerDashboard; 