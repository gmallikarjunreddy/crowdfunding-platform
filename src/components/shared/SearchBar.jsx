import React from 'react';

const SearchBar = () => {
    return (
        <div>
            <input type="text" placeholder="Search campaigns..." />
            {/* Add category filters and advanced search options */}
        </div>
    );
};

export default SearchBar; 