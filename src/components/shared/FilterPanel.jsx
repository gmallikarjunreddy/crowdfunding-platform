import React from 'react';

const FilterPanel = () => {
    return (
        <div>
            <h3>Filters</h3>
            {/* Add category selection, price range, and sort options */}
        </div>
    );
};

export default FilterPanel; 